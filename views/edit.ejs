<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Update Campground</title>
</head>

<body>
    <form action="/campgrounds/<%= camp._id%>?_method=PUT" method="POST">
        <div><label for="camp_name">Camp Name</label>
            <input type="text" id="camp_name" name="campground[title]" value="<%= camp.title %>">
        </div>
        <div><label for="camp_loc">Camp Location</label>
            <input type="text" id="camp_loc" name="campground[location]" value="<%= camp.location %>">
        </div>
        <button>Update Campground!</button>
    </form>
    <a href="/campgrounds">Back to Campgrounds</a>
</body>

</html>